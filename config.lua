
config = {

 	message = {
		poll = 120	-- Keep alive timer (seconds)
		, scur = 0	-- Secure connection
		, rcon = 1	-- auto-reconnect
		, qoss = 0	-- Sub QoS
		, qosp = 0	-- Pub QoS
		, retn = 1	-- Retain
	}
}
